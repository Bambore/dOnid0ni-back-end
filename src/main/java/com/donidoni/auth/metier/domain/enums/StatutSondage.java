package com.donidoni.auth.metier.domain.enums;

/** État d'un sondage « Quel groupage lancer ? ». */
public enum StatutSondage {
    /** Les votes sont acceptés. */
    OUVERT,
    /** Les votes sont clos, les résultats sont figés. */
    CLOS
}
